//SUBMIT A LINK TO THE GITHUB REPOSITORY CONTAINING THIS ASSIGNMENT
// Create a .js file that satisfies assignment below
// Copy the final assignment text into this assignment to submit
// For each exercise you'll want to log the result to the console to check your work
//test
//1. Create a set of variables that define an animal (age, height, name, etc...). Must be at least 6 variables.
const aType = "My Dog"
const aName = "Luna"
const aColor = "is white"
const legs = "it has"
const nLegs = "4"
const lLegs = "legs"
const aDo = "and she runs fast"
const aBark = "and it barks when she is happy"

//console.log(fName + lName);
//console.log(hNumber + sName);
//console.log(state + zcode);


const myAnimal = `${aType} ${aName} ${aColor}, ${legs}
${nLegs} ${lLegs} ${aDo} ${aBark}`
console.log(myAnimal)

//2. Create a set of variables that define a vehicle. Must be at least 6 variables.

const myCar = "Ferrari"
console.log(myCar)

const cColor = "RED"
console.log(cColor)

const cCarType = "is Sport"
console.log(cCarType)

let  nDoors = 4
console.log(nDoors)

let cRuns = "Fast"
console.log(cRuns)

let cCost = 10000000
console.log(cCost)

